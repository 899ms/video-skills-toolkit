◆  Checking ready-project

Lint
  ℹ pointer_events_none: `#caption-mount` sets `pointer-events: none`. Elements matching this selector are harder to select in the Studio preview.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/index.html #caption-mount t=0s
    Fix: If these elements should be selectable in the Studio, remove `pointer-events: none` or move it to a wrapper that doesn't contain editable content.
  ℹ system_font_will_alias: Font families will be substituted at render time: 'menlo' → JetBrains Mono, 'consolas' → JetBrains Mono. The renderer maps these to bundled fonts for cross-platform consistency. Use the target font name directly for consistent preview and render results.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/index.html [data-composition-id] t=0s
  ℹ pointer_events_none: `#caption-overlay-root` sets `pointer-events: none`. Elements matching this selector are harder to select in the Studio preview.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/compositions/caption-overlay.html #caption-overlay-root t=0s
    Fix: If these elements should be selectable in the Studio, remove `pointer-events: none` or move it to a wrapper that doesn't contain editable content.
  ℹ pointer_events_none: `.caption-group` sets `pointer-events: none`. Elements matching this selector are harder to select in the Studio preview.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/compositions/caption-overlay.html .caption-group t=0s
    Fix: If these elements should be selectable in the Studio, remove `pointer-events: none` or move it to a wrapper that doesn't contain editable content.
  ℹ pointer_events_none: `#pip-overlay-root` sets `pointer-events: none`. Elements matching this selector are harder to select in the Studio preview.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/compositions/pip-overlay.html #pip-overlay-root t=0s
    Fix: If these elements should be selectable in the Studio, remove `pointer-events: none` or move it to a wrapper that doesn't contain editable content.
  ℹ pointer_events_none: `#studio-background-root` sets `pointer-events: none`. Elements matching this selector are harder to select in the Studio preview.
    /Users/bozhou/my/视频工程/video-skills-toolkit/skills/hyperframes-scene-animator/evals/fixtures/ready-project/compositions/studio-background.html #studio-background-root t=0s
    Fix: If these elements should be selectable in the Studio, remove `pointer-events: none` or move it to a wrapper that doesn't contain editable content.
  0 error(s), 0 warning(s), 6 info(s)

Runtime
  ◇ 0 errors, 0 warnings

Layout
  ◇ 0 issues across 9 sample(s)

Motion
  ◇ 0 errors, 0 warnings

Contrast
  ◇ 41/41 text checks pass WCAG AA

Snapshots
  ◇ disabled

◇  Check passed
[INFO] [Compiler] Fetched 4 font face(s) for "Consolas" from Google Fonts (cached to /Users/bozhou/.cache/hyperframes/fonts/consolas)
[INFO] [Compiler] Injected deterministic @font-face rules for 2 requested font families
