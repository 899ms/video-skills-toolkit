import type {CSSProperties} from "react";
import {Easing, interpolate, useCurrentFrame, useVideoConfig} from "remotion";

/** ChecklistPop —— 检查项逐条出现并划勾。适合 3–5 条交付前核对；不适合长任务管理。 */
export type ChecklistPopProps={title?:string;eyebrow?:string;items?:readonly string[];titleAt?:number;itemsAt?:number;stagger?:number;checkDelay?:number;width?:number;height?:number;fontFamily?:string;numberFontFamily?:string;theme?:Partial<typeof defaultTheme>;style?:CSSProperties};
const defaultTheme={text:"#2E2F3D",muted:"#7D7E8B",accent:"#9A8FDD",warm:"#D88E73",line:"rgba(46,47,61,.10)",glass:"rgba(255,255,255,.80)",border:"rgba(255,255,255,.92)",shadow:"0 20px 54px rgba(70,64,92,.12)"};const clamp={extrapolateLeft:"clamp",extrapolateRight:"clamp"}as const;
export const ChecklistPop=({title="上线前检查",eyebrow="PRE-FLIGHT CHECK",items=["关键信息一眼可读","字幕与画面节奏一致","声音层级清晰自然"],titleAt=0,itemsAt=.52,stagger=.72,checkDelay=.24,width=1300,height=700,fontFamily='"Noto Sans SC", "PingFang SC", sans-serif',numberFontFamily='"Space Grotesk", monospace',theme,style}:ChecklistPopProps)=>{const frame=useCurrentFrame();const{fps}=useVideoConfig();const t=frame/fps;const c={...defaultTheme,...theme};const titleIn=enter(t,titleAt,.45);const list=items.slice(0,5);const progress=enter(t,itemsAt+Math.max(0,list.length-1)*stagger+.5,.72);return <div style={{width,height,color:c.text,fontFamily,display:"flex",flexDirection:"column",gap:38,...style}}><div style={{opacity:titleIn}}><div style={{color:c.muted,fontFamily:numberFontFamily,fontSize:26,letterSpacing:".14em"}}>{eyebrow}</div><div style={{marginTop:10,fontSize:76,fontWeight:900}}>{title}</div></div><div style={{display:"flex",flexDirection:"column",gap:18}}>{list.map((item,i)=>{const at=itemsAt+i*stagger;const row=enter(t,at,.42);const check=enter(t,at+checkDelay,.38);return <div key={`${item}-${i}`} style={{height:105,padding:"0 30px",boxSizing:"border-box",display:"grid",gridTemplateColumns:"70px 1fr 60px",alignItems:"center",gap:18,borderRadius:30,background:c.glass,border:`1px solid ${c.border}`,boxShadow:c.shadow,opacity:row,translate:`0 ${interpolate(row,[0,1],[28,0])}px`}}><div style={{width:48,height:48,borderRadius:14,border:`3px solid ${c.accent}`,display:"flex",alignItems:"center",justifyContent:"center",color:c.accent,fontSize:34,fontWeight:900,scale:.7+check*.3}}>{check>.18?"✓":""}</div><div style={{fontSize:36,fontWeight:800}}>{item}</div><div style={{color:c.muted,fontFamily:numberFontFamily,fontSize:24}}>0{i+1}</div></div>})}</div><div style={{height:10,borderRadius:999,background:c.line,overflow:"hidden"}}><div style={{height:"100%",width:`${progress*100}%`,borderRadius:999,background:`linear-gradient(90deg,${c.accent},${c.warm})`}}/></div></div>};
const enter=(t:number,s:number,d:number)=>interpolate(t,[s,s+d],[0,1],{...clamp,easing:Easing.bezier(.16,1,.3,1)});
