import type {CSSProperties} from "react";
import {Easing, interpolate, useCurrentFrame, useVideoConfig} from "remotion";

/** MetricPulse —— 单项巨型指标 + 迷你趋势条。适合增长率/转化率；不适合多维报表。 */
export type MetricPulseProps={title?:string;value?:number;unit?:string;note?:string;trend?:readonly number[];titleAt?:number;valueAt?:number;trendAt?:number;noteAt?:number;width?:number;height?:number;fontFamily?:string;numberFontFamily?:string;theme?:Partial<typeof defaultTheme>;style?:CSSProperties};
const defaultTheme={text:"#2E2F3D",muted:"#7D7E8B",accent:"#9A8FDD",warm:"#D88E73",line:"rgba(46,47,61,.10)",glass:"rgba(255,255,255,.76)",border:"rgba(255,255,255,.92)",shadow:"0 28px 80px rgba(70,64,92,.14)"};const clamp={extrapolateLeft:"clamp",extrapolateRight:"clamp"}as const;
export const MetricPulse=({title="本周内容增长",value=286,unit="%",note="连续四周保持上升",trend=[42,58,54,67,74,82,96,112],titleAt=0,valueAt=.52,trendAt=1.08,noteAt=1.78,width=1660,height=700,fontFamily='"Noto Sans SC", "PingFang SC", sans-serif',numberFontFamily='"Space Grotesk", monospace',theme,style}:MetricPulseProps)=>{const frame=useCurrentFrame();const{fps}=useVideoConfig();const t=frame/fps;const c={...defaultTheme,...theme};const titleIn=enter(t,titleAt,.5);const valueIn=enter(t,valueAt,.66);const shown=Math.round(value*valueIn);const max=Math.max(1,...trend);const pulse=1+Math.sin(frame/15)*.008;return <div style={{width,height,display:"flex",flexDirection:"column",gap:28,color:c.text,fontFamily,...style}}>
  <div style={{fontSize:76,fontWeight:900,letterSpacing:"-.045em",opacity:titleIn}}>{title}</div>
  <div style={{flex:1,display:"grid",gridTemplateColumns:".94fr 1.06fr",gap:34}}>
    <div style={{borderRadius:44,background:c.glass,border:`1px solid ${c.border}`,boxShadow:c.shadow,display:"flex",flexDirection:"column",justifyContent:"center",alignItems:"center",opacity:valueIn,scale:.96+valueIn*.04}}><div style={{display:"flex",alignItems:"baseline",fontFamily:numberFontFamily,scale:pulse}}><span style={{fontSize:210,fontWeight:700,letterSpacing:"-.08em"}}>{shown}</span><span style={{fontSize:86,fontWeight:700,color:c.warm}}>{unit}</span></div><div style={{color:c.muted,fontSize:34,opacity:enter(t,noteAt,.46)}}>{note}</div></div>
    <div style={{borderRadius:44,background:c.glass,border:`1px solid ${c.border}`,boxShadow:c.shadow,padding:"48px 50px",boxSizing:"border-box",display:"flex",flexDirection:"column",justifyContent:"space-between"}}><div style={{color:c.muted,fontSize:30,fontWeight:700}}>近 {trend.length} 周趋势</div><div style={{height:360,display:"flex",alignItems:"end",gap:18,borderBottom:`1px solid ${c.line}`}}>{trend.map((v,i)=>{const p=enter(t,trendAt+i*.15,.5);return <div key={i} style={{flex:1,height:`${v/max*88}%`,borderRadius:"16px 16px 4px 4px",background:i>=trend.length-2?`linear-gradient(${c.warm},${c.warm}88)`:`linear-gradient(${c.accent},${c.accent}72)`,scale:`1 ${p}`,transformOrigin:"center bottom",boxShadow:i===trend.length-1?`0 0 28px ${c.warm}44`:undefined}}/>})}</div></div>
  </div></div>};
const enter=(t:number,s:number,d:number)=>interpolate(t,[s,s+d],[0,1],{...clamp,easing:Easing.bezier(.16,1,.3,1)});
