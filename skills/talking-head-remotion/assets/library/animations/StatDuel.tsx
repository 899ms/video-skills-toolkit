import type {CSSProperties} from "react";
import {Easing, interpolate, useCurrentFrame, useVideoConfig} from "remotion";

/** StatDuel —— 两个接近指标的圆环对决。适合同口径双数据；不适合没有可比性的指标。 */
export type StatDuelProps={title?:string;leftLabel?:string;leftValue?:number;rightLabel?:string;rightValue?:number;unit?:string;operator?:string;verdict?:string;titleAt?:number;leftAt?:number;rightAt?:number;operatorAt?:number;verdictAt?:number;width?:number;height?:number;fontFamily?:string;numberFontFamily?:string;theme?:Partial<typeof defaultTheme>;style?:CSSProperties};
const defaultTheme={text:"#2E2F3D",muted:"#7D7E8B",accent:"#9A8FDD",warm:"#D88E73",line:"rgba(46,47,61,.12)",glass:"rgba(255,255,255,.76)",border:"rgba(255,255,255,.92)",shadow:"0 28px 80px rgba(70,64,92,.14)"};const clamp={extrapolateLeft:"clamp",extrapolateRight:"clamp"}as const;
export const StatDuel=({title="湿度数据对决",leftLabel="上海",leftValue=92,rightLabel="亚马逊",rightValue=90,unit="%",operator=">",verdict="上海湿度更高",titleAt=0,leftAt=.42,rightAt=.92,operatorAt=1.42,verdictAt=2.02,width=1480,height=620,fontFamily='"Noto Sans SC", "PingFang SC", sans-serif',numberFontFamily='"Space Grotesk", monospace',theme,style}:StatDuelProps)=>{const frame=useCurrentFrame();const{fps}=useVideoConfig();const t=frame/fps;const c={...defaultTheme,...theme};const titleIn=enter(t,titleAt,.45);const leftIn=enter(t,leftAt,.65);const rightIn=enter(t,rightAt,.65);const opIn=enter(t,operatorAt,.35);return <div style={{width,height,color:c.text,fontFamily,display:"flex",flexDirection:"column",alignItems:"center",gap:36,...style}}><div style={{fontSize:72,fontWeight:900,opacity:titleIn}}>{title}</div><div style={{flex:1,display:"grid",gridTemplateColumns:"1fr 130px 1fr",alignItems:"center",width:"100%"}}><Gauge label={leftLabel} value={leftValue} unit={unit} p={leftIn} color={c.accent} c={c} numberFontFamily={numberFontFamily} frame={frame}/><div style={{width:84,height:84,borderRadius:26,display:"flex",alignItems:"center",justifyContent:"center",background:c.glass,border:`1px solid ${c.border}`,boxShadow:c.shadow,color:c.warm,fontFamily:numberFontFamily,fontSize:52,fontWeight:700,opacity:opIn,scale:.75+opIn*.25}}>{operator}</div><Gauge label={rightLabel} value={rightValue} unit={unit} p={rightIn} color={c.warm} c={c} numberFontFamily={numberFontFamily} frame={frame}/></div><div style={{padding:"14px 28px",borderRadius:999,background:c.glass,border:`1px solid ${c.border}`,fontSize:30,fontWeight:800,opacity:enter(t,verdictAt,.42)}}>{verdict}</div></div>};
const Gauge=({label,value,unit,p,color,c,numberFontFamily,frame}:{label:string;value:number;unit:string;p:number;color:string;c:typeof defaultTheme;numberFontFamily:string;frame:number})=><div style={{display:"flex",flexDirection:"column",alignItems:"center",gap:18,opacity:p}}><div style={{position:"relative",width:310,height:310,borderRadius:"50%",display:"flex",alignItems:"center",justifyContent:"center",background:c.glass,border:`9px solid ${color}77`,boxShadow:`${c.shadow},0 0 ${26+Math.sin(frame/14)*6}px ${color}44`,scale:.82+p*.18}}><div style={{display:"flex",alignItems:"baseline",fontFamily:numberFontFamily}}><span style={{fontSize:128,fontWeight:700,color}}>{Math.round(value*p)}</span><span style={{fontSize:48,fontWeight:700}}>{unit}</span></div></div><div style={{fontSize:32,fontWeight:800}}>{label}</div></div>;
const enter=(t:number,s:number,d:number)=>interpolate(t,[s,s+d],[0,1],{...clamp,easing:Easing.bezier(.16,1,.3,1)});
